# First Ten Minutes

The current v0.6 first-use path is maintained in
[five-minute offline orientation](offline-orientation.md) and the
[installed-wheel tutorial](tutorial-v0.6/README.md).

Start with:

```text
cpcf agent explain --json
cpcf self-check --json
cpcf schema list --json
```

Contributors use:

```text
uv sync --frozen --all-extras --group dev --group security
```
