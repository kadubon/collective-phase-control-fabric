# CPCF API

FastAPI reference control plane for CPCF v0.6. It provides OIDC-authenticated tenant APIs,
optimistic generation checks, PostgreSQL RLS integration, immutable jobs, and stable response
envelopes. It never executes customer adapter code.

This package is a release-gated reference implementation and does not establish deployment assurance.
